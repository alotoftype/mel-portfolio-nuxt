[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24"
  NITRO_PRESET = "netlify"

[functions]
  directory = ".netlify/functions-internal"

[dev]
  command = "npm run dev"
  port = 8888
  targetPort = 3000
  autoLaunch = false
